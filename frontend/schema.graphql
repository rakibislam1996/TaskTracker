# Placeholder; should be replaced by introspected schema via curl or relay-compiler script.
# Run after backend is up:
# curl -X POST http://localhost:8080/graphql -H "Content-Type: application/json" \
#   -d '{"query":"{ __schema { types { name } } }"}'

scalar UUID

enum TaskStatus { Pending Completed }

type TaskItem {
  id: UUID!
  title: String!
  description: String
  status: TaskStatus!
  createdAt: String!
  updatedAt: String!
}

type Query {
  getAllTasks: [TaskItem!]!
}

type Mutation {
  createTask(title: String!, description: String): TaskItem!
  updateTaskStatus(id: UUID!, status: TaskStatus!): TaskItem!
}
