# Task Tracker GraphQL Schema
# Auto-generated from backend implementation

scalar UUID
scalar DateTime

enum TaskStatus { 
  Pending 
  Completed 
}

type TaskItem {
  id: UUID!
  title: String!
  description: String
  status: TaskStatus!
  createdAt: DateTime!
  updatedAt: DateTime!
}

type Query {
  getAllTasks: [TaskItem!]!
}

type Mutation {
  createTask(title: String!, description: String): TaskItem!
  updateTaskStatus(id: UUID!, status: TaskStatus!): TaskItem!
}
